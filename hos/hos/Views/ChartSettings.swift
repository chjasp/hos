import SwiftUI


/*
 Manage formula on which's basis the plotted data is calculated
 */
struct ChartSettings: View {
    var body: some View {
        Text("Edit formula")
    }
}

struct ChartSettings_Previews: PreviewProvider {
    static var previews: some View {
        ChartSettings()
    }
}
